# E2E Helpers

Playwright helpers live here once canvas pixel checks and project fixtures are implemented.

Rules:

- Use temporary project roots only.
- Exercise local bridge mock adapters instead of browser-side route mocks.
- Keep real Codex, Claude, ffmpeg, Basic Pitch, ACE-Step, GPU, and network out of default E2E.
