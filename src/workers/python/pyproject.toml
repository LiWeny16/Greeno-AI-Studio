[project]
name = "cc-music-workers"
version = "0.0.0"
description = "Optional Python workers for CC Music post-MVP adapters."
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = []

[tool.uv]
package = false
